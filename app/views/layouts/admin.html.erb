<!DOCTYPE html>
<html>
  <head>
    <title>Admin-Volleyball_and_you</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_pack_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <header>
      <nav class="navbar navbar-expand-lg navbar-dark bg-primary"> 
        <div class="container text-center">
          <h2>管理者画面</h2>
            <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavDropdown"
                        aria-controls="#navbarNavDropdown" aria-expanded="true" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
          </button>
          <div class="navbar-collapse collapse" id="navbarNavDropdown">
            <ul class="navbar-nav ml-auto">
              <% if admin_signed_in? %>
                  
                  <li class="nav-item">
                    <%= link_to "ユーザー一覧", admin_dashboards_path, class: 'nav-link text-lite' %>
                  </li>
                  
                  <li class="nav-item">
                    <%= link_to "投稿一覧", admin_posts_path, class: 'nav-link text-lite' %>
                  </li>
                  
                  <li class="nav-item">
                    <%= link_to "ログアウト", destroy_admin_session_path, method: :delete, class: 'nav-link text-lite' %>
                  </li>
                  
                  <% else %>
                  
                  <li class="nav-item">
                    <%= link_to "ログイン", new_admin_session_path, class: 'nav-link text-lite' %>
                  </li>
               <% end %>
              </div>
            </div>
          </nav>
        </header>
       <main>
         <%= yield %>
        </main>
     </body>
     
      <footer class="text-center" style="background-color: #afafb0;">
        <div class="container">
          <div class="row">
            <p>©Volleyball_and_you</p>
          </div>
        </div>
      </footer>
   </html>
