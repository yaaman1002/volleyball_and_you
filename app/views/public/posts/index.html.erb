<div class="col-md-2"></div>
  <nav>
    <ul class="side-bar">
      
    </ul>
  </nav>
いいね一覧
フォロー一覧
（バレーボール基礎知識情報）

<div class="aboutback">
  <div class="text-center mb" style="background-color: #cee4ae;">
      <h1 class="text-center">My_page</h1>
          <%= image_tag @user.get_profile_image(100,100) %>
            <table class="table table-striped col-md-5">
              <tbody class="text-center">
                <tr></tr>
                 <tr><th class="bg-lite">name</th>
                     <td><%= @user.name %></td></tr>
                 <tr><th class="bg-lite">introduction</th>
                     <td><%= @user.introduction %></td></tr>
              </tbody>
            </table>
              <%= link_to edit_user_path(@user), class: 'btn btn-outline-primary' do %>
                <i class="fa-solid fa-user-gear"></i>
              <% end %>
       </div>
           <div class="text-center">
             <table class="table table-striped col-md-7">
               <thead>
               <tr>
                <th></th>
                <th>タイトル</th>
                <th>投稿内容</th>
                <th colspan="3"></th>
               </tr>
              <tbody>
                 <% @posts.each do |posts| %>
                    <tr>
                      <td>
                        <%= link_to user_path(@user) do %>
                        <%= image_tag(posts.get_image, size: "150x150") %>
                        <% end %>
                      </td>
                      <td>
                        <%= link_to posts.title, user_path(@user) %>
                      </td>
                      <td>
                        <%= link_to posts.body %>
                      </td>
                    </tr>
                    <% end %>
                  </tbody>
                </thead>
              </table>
            </div>
          </div>
        
      
    